#!/bin/bash

BASEDIR=$(dirname "$(which "$0")")
PROJDIR=$(dirname "$BASEDIR" )


export PYTHONPATH=$PROJDIR/src:$PROJDIR/src/pymir:$PYTHONPATH


if [ $# -ne 1 ]
then
    echo "Usage: "$0" name_of_sample"
    exit 0
fi

python "$PROJDIR/src/linne/analyzer/cmd/cutter2.py" $1
